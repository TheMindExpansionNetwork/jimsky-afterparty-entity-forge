# Afterparty Forge Payload

A cloneable entity payload for Hermes/Jimsky: identity, safety, logo dataset seed, and build pipeline.

Install only into a Hermes profile after reviewing `manifest.json`. Secrets stay outside the payload.
